alter table APP.TRADE_HISTORY add column PORTFOLIO_NAME varchar(20);
select * from APP.TRADE_HISTORY
select PORTFOLIO_ID, TICKER_SYMBOL, COMPANY_NAME, BETA_VALUE from SECURITIES where SELECTED = true